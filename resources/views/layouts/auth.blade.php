@include('partials.auth._header')

<body>
    @inertia
    {{-- <section class="ftco-section">
        <div class="container">
            @yield('container')
        </div>
    </section> --}}

    @include('partials.auth._footer')

</body>

</html>
